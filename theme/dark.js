import darkCursor from '../public/darkCursor.png';
import lightCursor from '../public/lightCursor.png';

const dark = {
  primary: '#17182F',
  secundary: '#FDFBF5',
  timeColor: '#FDFBF5',
  cursor: `url(${darkCursor}) 6 6, auto`,
  invertedCursor: `url(${lightCursor}) 6 6, auto`,
};

export default dark;
